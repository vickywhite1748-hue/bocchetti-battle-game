# 战斗吧！Bocchetti！

《战斗吧！Bocchetti！》是一款基于积点和角色拍立得的网页桌游灰盒版本。当前包含和平模式和竞争模式两个单机玩法。

游玩地址：

https://vickywhite1748-hue.github.io/bocchetti-battle-game/

## 当前版本

- 当前最新版本：`v1.5.3`。
- 支持 2-4 人单机对局。
- 支持 AI 自动参与。
- 和平模式支持角色拍立得、积点、签署拍立得、观众技能和一轮内人物羁绊。
- 竞争模式支持公开拍立得、独立积点、秘密登记、跨小局羁绊和目标分。
- 当前仍是规则灰盒版本，后续会继续接入正式立绘和更完整的视觉表现。

## 更新记录

### v1.5.3

- 竞争模式对局页新增“重开”按钮，游戏结束后可直接回到竞争模式设置页重新开始。

### v1.5.2

- 修正和平模式辅助分析关闭后，页面顶部仍显示建议提示的问题。
- 公开页面重新构建并发布到 GitHub Pages，确保线上版本跟随本次更新。

### v1.5.1

- 新增单机辅助分析，和平模式手牌和竞争模式公开拍立得可显示最终成功率、期望分、风险标签和建议动作。
- 辅助分析默认关闭，仅在单机对局内手动开启；主页不显示，不会读取其他玩家秘密登记。
- 结算阶段不再允许选择未满足条件的拍立得作为计分牌，避免误结算 0 分。
- 家族荣光在 AI 和辅助估值中按 8 分参考值处理；和平模式特殊胜利和竞争模式 8 分结算规则保持不变。
- 优化辅助界面：按钮横向排列，去除重复提示，拍立得条件中的积点标记与上方积点颜色保持一致。

### v1.5.0

- 新增游戏主页，提供“和平模式”和“竞争模式”两个入口。
- 和平模式保留原有公共积点、弃置、签署、观众技能、一轮内羁绊和目标分玩法。
- 新增竞争模式单机原型：公开拍立得、独立积点、秘密登记、4 回合小局和目标分。
- 竞争模式通过“抽取积点”按钮手动切换回合；全员登记后仍保留 4 回合节奏，第 4 回合后结算。
- 竞争模式比分卡会显示每位观众已达成的拍立得，上一小局结算不会在下一小局继续占用主界面。
- 竞争模式家族荣光按 8 分高分牌结算，不直接胜利。
- 竞争模式采用跨小局羁绊，羁绊奖励统一为 +1，且与和平模式成就独立。
- 人物卡文本已同步：删除“桥上的情侣”，新增“讨厌的哥哥”，Botti《罗密欧与朱丽叶》更名为《幼小的妹妹》。

### v1.4.0

- 新增对局页阶段提示，直接说明当前该做什么。
- 优化游戏规则弹窗，把首局必要信息放在基本规则。
- 补充积点、拍立得条件、签署、羁绊、家族荣光和观众技能说明。
- 按钮不可操作时显示更明确的原因提示。
- 自动弹出的更新记录只显示最新版；手动点击“更新记录”显示最近三个版本。

### v1.3.0

- 新增页面内“反馈”入口，启动页和对局页都可以打开。
- 打开反馈时会自动复制版本、页面、阶段、轮数、角色、比分和积点等信息。
- 反馈浮窗提供“复制自动信息”和“显示自动信息”兜底操作。
- 当前反馈提交入口为腾讯问卷内嵌表单，并保留新窗口打开兜底。

### v1.2.0

- Michele & Paulo 拆为 Mighele《草帽小红》和 Paulo《草帽小绿》。
- 新增羁绊“赌场主理人”和“父母爱情”。
- 新增“家族荣光”胜利牌。
- 新增“羁绊成就”，未解锁剧情显示为 `？？？`。
- 达成羁绊时显示 2 秒解锁提示。
- 特殊规则页只保留羁绊玩法说明。
- 新增 `A = B` 型拍立得条件。
- 调整多张拍立得条件与分值，`Gambino《赌场陷阱》` 改为 5 分命运牌。
- 新增页面内“更新记录”入口，同版本关闭后不再自动提示。

## 本地运行

```bash
npm install
npm run dev
```

## 构建检查

```bash
npm test
npm run build
```

## 操作说明

1. 选择对局人数和观众角色。
2. 每轮抽取积点。
3. 根据当前积点趋势弃置不合适的角色拍立得。
4. 第二次弃置阶段可以签署 1 张角色拍立得。
5. 最终保留 2 张角色拍立得，并选择 1 张满足条件的拍立得计分。
6. 若你的拍立得中两张形成羁绊，卡面会显示羁绊名称；结算时计分拍立得满足条件即可获得羁绊分。

## 部署

当前公开试玩版发布在 `gh-pages` 分支。发布前先构建：

```bash
set VITE_BASE_PATH=/bocchetti-battle-game/
npm run build
```

然后将 `dist/` 内容推送到 `gh-pages` 分支，并在仓库 Pages 设置中选择 `gh-pages` 分支根目录作为发布来源。
